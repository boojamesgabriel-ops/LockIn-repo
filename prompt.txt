You are tasked with debugging my Chrome/Brave extension project called LockIn.  
I will provide you with the relevant files: background.js, script.js, and popup.html.  
Your job is to analyze why none of the popup buttons (.fifteenMin-btn, .thirtyMin-btn, .fortyfiveMin-btn, .sixtyMin-btn, .start-btn) respond when clicked.

### Context
- manifest.json correctly declares background.js as a service worker and includes "storage" permission.
- background.js listens for "saveState" and "loadState" messages and logs them successfully.
- script.js defines `initButtonHandlers()` which attaches click listeners to the buttons.
- script.js is loaded at the bottom of popup.html before </body>.
- When async functions (`saveState()` / `loadState()`) are added, buttons stop working.
- When async is removed, buttons respond.

### Debugging Tasks
1. Verify that `initButtonHandlers()` is being called when popup.html loads.
2. Confirm that `querySelectorAll(".fifteenMin-btn, .thirtyMin-btn, .fortyfiveMin-btn, .sixtyMin-btn")` actually finds the buttons in popup.html.
3. Add logging to show when each button is clicked and what duration value is mapped.
4. Check if `saveState()` or `loadState()` calls are hanging due to missing `return true` in background.js listener.
5. Ensure async/await usage in script.js does not block UI updates — suggest proper try/catch and logging.
6. Provide corrected versions of script.js and background.js with inline comments explaining fixes.

### Output
- Show the corrected `initButtonHandlers()` function with proper logging and async handling.
- Show the corrected `saveState()` and `loadState()` functions in script.js.
- Show the corrected background.js listener ensuring async responses work.
- Include inline comments like `// FIXED: added return true to keep async channel open`.

